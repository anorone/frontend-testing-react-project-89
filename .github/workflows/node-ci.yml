---
name: node CI
on: push
jobs:
  run-check:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v5
      - name: prepare
        run: npm ci
      - name: check
        run: |
          npm run lint
          npm run test
